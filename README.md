# 练伴

一个适配手机浏览器的健身打卡网页原型，支持每日打卡、历史补签、训练计划、好友互动、排行榜、体重趋势和本地数据保存。

## 运行

直接用浏览器打开 `index.html`，或在当前目录启动任意静态文件服务器。

例如：

```powershell
python -m http.server 8080
```

然后访问 `http://localhost:8080`。

## 数据说明

当前版本无需注册，数据保存在浏览器的 `localStorage` 中。清除网站数据后会恢复为初始演示数据。
